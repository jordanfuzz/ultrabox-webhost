on:
  push:
    branches:
      - master
  repository_dispatch:
    types: [default]
jobs:
  deploy:
    name: deploy
    runs-on: self-hosted
    steps:
      - name: build
        run: docker build -t ultrabox-webhost .
      - name: start nginx
        run: docker run -dp 80:80 --name ultrabox-webhost ultrabox-webhost
